{
    "success":true,
    "types":[{% if types %}{% for t in types %}
        "{{ t }}"{% if not forloop.last %},{% endif %}
    {% endfor %}{% endif %}],
    "categories":[{% if categories %}{% for c in categories %}
        "{{ c }}"{% if not forloop.last %},{% endif %}
    {% endfor %}{% endif %}],
    "has_complementaries":[{% if has_complementaries %}{% for h in has_complementaries %}
        "{{ h }}"{% if not forloop.last %},{%endif %}
    {% endfor %}{% endif %}],
    "complementaries":[{% if complementaries %}{% for c in complementaries %}
        "{{ c }}"{% if not forloop.last %},{% endif %}
    {% endfor %}{% endif %}]
}
